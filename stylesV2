/* ===== 布局基础 ===== */
.wrap {
  display: grid;
  grid-template-columns: 260px 1fr;
  min-height: 100dvh;
}

/* 左侧栏默认样式（桌面常显） */
aside {
  position: sticky;
  top: 0;
  height: 100dvh;
  overflow: auto;
  border-right: 1px solid rgba(0,0,0,.06);
  background: #fff;
  z-index: 20;
}

/* 主区 */
main { position: relative; }

/* 浮动按钮（默认隐藏，移动端显示） */
.fab-menu {
  position: fixed;
  right: max(16px, env(safe-area-inset-right));
  bottom: max(16px, env(safe-area-inset-bottom));
  width: 56px; height: 56px;
  border-radius: 50%;
  border: none;
  background: #2f573f;
  box-shadow: 0 8px 24px rgba(0,0,0,.18);
  z-index: 60;
  display: none;
  align-items: center; justify-content: center;
}
.fab-menu img { width: 28px; height: 28px; }

/* 遮罩层 */
.backdrop {
  position: fixed; inset: 0;
  background: rgba(0,0,0,.35);
  backdrop-filter: blur(2px);
  z-index: 50;
}

/* ====== 响应式：平板/手机把 aside 变为抽屉 ====== */
@media (max-width: 1024px) {
  .wrap {
    grid-template-columns: 1fr;         /* 不再占固定列 */
  }
  aside {
    position: fixed;
    inset: 0 auto 0 0;                  /* 左侧贴边 */
    width: min(86vw, 320px);
    transform: translateX(-100%);
    transition: transform .28s ease;
    box-shadow: 2px 0 24px rgba(0,0,0,.18);
  }
  /* 打开状态（给 body 加 .menu-open） */
  body.menu-open aside { transform: translateX(0); }
  /* 显示遮罩与按钮 */
  .fab-menu { display: inline-flex; }
  body.menu-open .backdrop[hidden] { display: block !important; } /* 保险 */
}

/* 减少动画偏好 */
@media (prefers-reduced-motion: reduce) {
  aside { transition: none !important; }
}
